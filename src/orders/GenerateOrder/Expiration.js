import Section, { SectionLabel } from "../../common/FormSection";

export default () => (
  <Section>
    <SectionLabel>Expiration Date and Time</SectionLabel>

    {/* TODO - datapicker -> expirationUnixTimestampSec */}
    <div>datepicker here</div>
  </Section>
);
